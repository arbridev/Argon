# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Argon' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Argon
  pod 'Alamofire', '~> 5.2'
  pod 'Kingfisher', '~> 6.0'

  target 'ArgonTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ArgonUITests' do
    # Pods for testing
  end

end
